# Only fail CI on high/critical vulnerabilities.
# The 2 moderate findings are in Next.js's own bundled postcss (build-time only)
# and cannot be fixed without downgrading Next.js. Track upstream:
# https://github.com/advisories/GHSA-qx2v-qp2m-jg93
audit-level=high
